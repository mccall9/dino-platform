{
  "name": "dino-platform",
  "private": true,
  "description": "Future monorepo for dino.blog — Bun, Turborepo, Elysia, Eden Treaty, TanStack Start, Tailwind. Product migration from static build-in-public comes next.",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:api": "bun run --filter @dino/api dev",
    "dev:web": "bun run --filter @dino/web dev",
    "build": "turbo run build",
    "build:vercel": "cd apps/web && bun run build && cd ../.. && node scripts/copy-vercel-output.mjs",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "smoke:api": "bun run scripts/smoke-api.ts"
  },
  "devDependencies": {
    "turbo": "^2.10.5",
    "typescript": "^5.8.3"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.1.0"
  }
}
